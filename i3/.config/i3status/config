general {
    colors = true
    interval = 5
}

battery all {
    format = "%percentage"
}

tztime local {
    format = "%a %m/%d %H:%M "
    separator_block_width = 1
}

order += "battery all"
order += "tztime local"
